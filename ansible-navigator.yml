---
ansible-navigator:
  editor:
    command: charm --line {line_number} {filename}
  execution-environment:
    container-engine: docker
    enabled: true
    image: aap-pah.apps.pinguozzo.com/custom-build-24
    pull:
      arguments:
      - --tls-verify=false
      - --creds=admin:sh4dowm4n
      policy: missing
  logging:
    append: true
    level: critical
  playbook-artifact:
    enable: true
