---
rhc_auth:
  hostgroup: "ESX RHEL9 NET_apps"
  activation_keys:
    keys:
      - "ak-rhel9-vmware"

hg_name: "{{ hg | default('ESX RHEL9 NET_apps') }}"
os: "{{ operatingsystem | default('RedHat 9.0') }}"
ks: "Red Hat Enterprise Linux 9 for x86_64 - BaseOS Kickstart 9.0"
content_source: "{{ satellite_server }}" 
domain_name: "{{ domain |default('apps.pinguozzo.com') }}"
